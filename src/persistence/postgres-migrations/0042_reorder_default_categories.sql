-- Intentionally a no-op. Reordering every category with a preset name would
-- overwrite user-defined ordering. Existing ledgers change order only when a
-- user applies a preset or explicitly reorders categories.
SELECT 1;
